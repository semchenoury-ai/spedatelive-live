name: keepalive
on: {schedule: [{cron: "*/5 9-23 * * *"}, {cron: "*/5 0-1 * * *"}], workflow_dispatch: {}}
jobs: {ping: {runs-on: ubuntu-latest, steps: [{run: "curl -s -m 30 https://speedatelive.com/health || true"}]}}
