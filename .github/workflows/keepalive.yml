name: keepalive
on: {schedule: [{cron: "*/5 * * * *"}], workflow_dispatch: {}}
jobs: {ping: {runs-on: ubuntu-latest, steps: [{run: "curl -s -m 30 https://spedatelive-live.onrender.com/health || true"}]}}
